import styled from 'styled-components';
import PropTypes from 'prop-types';

const SideMarginRegulator = styled.div`
  margin: 0 8.5px;
`;

SideMarginRegulator.propTypes = {};

export default SideMarginRegulator;
